import React from "react";
import { CategoryMemoriesScreen } from "./CategoryMemoriesScreen";

export const MemoryListScreen = (props: any) => {
  return <CategoryMemoriesScreen {...props} />;
};

export default MemoryListScreen;
